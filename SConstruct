import os
import re
import sys
from config import *

group = []
# /app_update
name = 'app_update'
path = os.path.join(COMPONENT_DIR, name)
CPPPATH = ['include']
group.append({
	'name': name,
	'path': path,
	'CPPPATH': CPPPATH
	})
print group

# /bootloader
